version: "3.9"

services:
  react-app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        REACT_APP_API_URL: ${REACT_APP_API_URL}
        REACT_APP_GOOGLE_API: ${REACT_APP_GOOGLE_API}
    ports:
      - "3000:80"
    env_file:
      - .env